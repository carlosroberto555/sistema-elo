import styled from "styled-components";

export const ChatsContainer = styled.div`
  position: fixed;
  bottom: 0;
  right: 0;
  height: 400px;
  display: flex;
  flex-direction: row;
  align-items: flex-end;
`;
